{
  "name": "cingaryee-daily-lectionary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node --test tests/site.test.mjs"
  }
}
